'use client';
import { Container, Typography } from "@mui/material";

export default function TimesheetsPage() {
    return (
        <Container>
            <Typography variant='h4' color='secondary' textAlign='center'>Click or create a timesheet to get started!</Typography>
        </Container>
    );
}
